<h1>Users</h1>

<ul>
    <% @users.each do |user| %>
        <li> 
            <%= link_to user.firstName+" "+user.lastName, user %> 
        </li>
    <% end %>
</ul>

<%= link_to "Create New User", new_user_path %>
